import { DecimalFixPipe } from './decimal-fix.pipe';

describe('DecimalFixPipe', () => {
  it('create an instance', () => {
    const pipe = new DecimalFixPipe();
    expect(pipe).toBeTruthy();
  });
});
